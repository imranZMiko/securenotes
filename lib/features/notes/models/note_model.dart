class Note {
  String title;
  String content;
  DateTime created;
  DateTime lastModified;

  Note({
    required this.title,
    required this.content,
    required this.created,
    required this.lastModified,
  });
}
